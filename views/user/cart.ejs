<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mon Panier</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-800">

    <header class="bg-white shadow-md p-4 flex justify-between items-center">
        <h1 class="text-2xl font-bold">Mon Panier</h1>
        <a href="/user/home" class="text-blue-500 hover:text-blue-700 transition duration-200">
            &larr; Continuer mes achats
        </a>
    </header>

    <main class="container mx-auto p-4 md:p-8">
        <% if (panier && panier.length > 0) { %>
            <div class="bg-white p-6 rounded-lg shadow-lg">
                <div class="flex justify-between items-center mb-6 border-b pb-4">
                    <h2 class="text-2xl font-semibold">Articles du panier</h2>
                    <form action="/api/cart/clear?_method=DELETE" method="POST" onsubmit="return confirm('Êtes-vous sûr de vouloir vider votre panier ?');">
                        <button type="submit" class="bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded-full transition duration-300">
                            Vider le panier
                        </button>
                    </form>
                </div>

                <div class="divide-y divide-gray-200">
                    <% panier.forEach(item => { %>
                        <div class="flex items-center py-4">
                            <img src="<%= item.product.image %>" alt="<%= item.product.name %>" class="w-24 h-24 object-cover rounded-lg mr-6 shadow-md">
                            <div class="flex-grow">
                                <h3 class="font-bold text-lg"><%= item.product.name %></h3>
                                <p class="text-gray-600">Prix unitaire: <span class="font-semibold"><%= item.product.price.toFixed(2) %> €</span></p>
                                <p class="text-gray-600">Quantité: <span class="font-semibold"><%= item.quantity %></span></p>
                            </div>
                            <div class="flex flex-col items-end space-y-2">
                                <p class="text-lg font-bold text-blue-600">
                                    <%= (item.product.price * item.quantity).toFixed(2) %> €
                                </p>
                                <form action="/api/cart/remove/<%= item.product._id %>?_method=DELETE" method="POST" onsubmit="return confirm('Êtes-vous sûr de vouloir retirer ce produit ?');">
                                    <button type="submit" class="bg-red-500 hover:bg-red-600 text-white font-bold py-1 px-3 rounded-full text-sm transition duration-300">
                                        Retirer
                                    </button>
                                </form>
                            </div>
                        </div>
                    <% }); %>
                </div>

                <div class="mt-8 pt-4 border-t-2 border-gray-300 text-right">
                    <p class="text-xl font-bold text-gray-900">Total général: <span class="text-green-600 text-2xl"><%= totalGeneral.toFixed(2) %> €</span></p>
                    <button class="mt-4 bg-green-500 hover:bg-green-600 text-white font-bold py-3 px-6 rounded-full transition duration-300">
                        Passer la commande
                    </button>
                </div>
            </div>

        <% } else { %>
            <div class="text-center p-8 bg-white rounded-lg shadow-lg">
                <p class="text-xl text-gray-500 mb-4">Votre panier est vide.</p>
                <a href="/user/home" class="inline-block bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-full transition duration-300">
                    Retourner à la boutique
                </a>
            </div>
        <% } %>
    </main>

    <footer class="bg-white mt-8 p-4 text-center text-gray-600 shadow-inner">
        <p>&copy; 2025 Ma Boutique</p>
    </footer>

</body>
</html>