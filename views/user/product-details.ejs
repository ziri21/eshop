<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %> - Détails</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-800">
    
    <header class="bg-white shadow p-4">
        <div class="container mx-auto max-w-6xl">
            <a href="/user/home" class="text-blue-500 hover:text-blue-700 transition duration-200">&larr; Retour à la boutique</a>
        </div>
    </header>

    <main class="container mx-auto p-4 md:p-8 mt-6 max-w-6xl">
        <div class="bg-white p-6 md:p-10 rounded-lg shadow-lg grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12">
            <div class="product-image-container">
                <% if (product.image) { %>
                    <img src="<%= product.image %>" alt="<%= product.name %>" class="w-full h-auto object-cover rounded-lg shadow-md">
                <% } else { %>
                    <div class="w-full h-80 bg-gray-200 flex items-center justify-center rounded-lg text-gray-500 shadow-md">
                        <p>Aucune image disponible</p>
                    </div>
                <% } %>
            </div>
            
            <div class="product-info flex flex-col justify-start">
                <h2 class="text-3xl md:text-4xl font-extrabold text-gray-900 mb-4"><%= product.name %></h2>
                
                <p class="text-gray-700 mb-6 leading-relaxed"><%= product.description %></p>
                
                <p class="text-3xl font-bold text-blue-600 mb-6">
                    <%= product.price %> €
                </p>

                <form action="/api/cart/add" method="POST" class="flex items-end space-x-4">
                    <input type="hidden" name="product" value="<%= product._id %>">
                    
                    <div>
                        <label for="quantity" class="block text-sm font-medium text-gray-700">Quantité :</label>
                        <input type="number" id="quantity" name="quantity" value="1" min="1" required 
                               class="mt-1 block w-20 rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring focus:ring-blue-200 focus:ring-opacity-50 transition duration-200">
                    </div>
                    
                    <button type="submit" class="bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-6 rounded-full transition duration-300 transform hover:scale-105">
                        Ajouter au panier
                    </button>
                </form>
            </div>
        </div>
    </main>

    <footer class="bg-white mt-8 p-4 text-center text-gray-600 shadow-inner">
        <p>&copy; 2025 Ma Boutique</p>
    </footer>

</body>
</html>